import { ReactNode } from 'react'

/** Shop dùng layout trang thường — nội dung xếp dọc, cuộn trang tự nhiên. */
export default function ShopLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
